services:
  subfeed-extractor:
    build: .
    image: subfeed-extractor:latest
    container_name: subfeed-extractor
    restart: unless-stopped
    ports:
      - "8787:8787"
    environment:
      # 앱 설정의 shared secret 과 동일하게. 빈 값이면 인증 생략(비권장).
      SUBFEED_SECRET: "${SUBFEED_SECRET:-}"
      BGUTIL_BASE_URL: "http://bgutil-provider:4416"
      TOKEN_TTL_SECONDS: "7200"
      RATE_LIMIT_PER_HOUR: "30"
      # YouTube 정책 변동 시 조정 (기본: mweb + bgutil). 자세한 건 README "환경 변수".
      # YT_PLAYER_CLIENT: "mweb"
      # USE_BGUTIL: "true"
      # YT_FORMAT: "best[ext=mp4][acodec!=none][vcodec!=none]/best[acodec!=none][vcodec!=none]"
      # PUBLIC_BASE_URL: ""
    depends_on:
      - bgutil-provider

  bgutil-provider:
    image: brainicism/bgutil-ytdlp-pot-provider:latest
    container_name: bgutil-provider
    restart: unless-stopped
    # 내부 네트워크에서 subfeed-extractor 가 http://bgutil-provider:4416 으로 접근.
    # 외부 포트 노출 불필요.
    expose:
      - "4416"
